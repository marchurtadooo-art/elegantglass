# GLASSWORK — backend dependencies (clean minimal set for Render).
#
# This file lists ONLY the packages directly imported by server.py,
# security.py, notifications.py and their transitive runtime needs.
# pip will resolve sub-dependencies automatically.
#
# Note: code uses PyJWT (`import jwt`), not python-jose.
# `pydantic[email]` enables `EmailStr`; we pin email-validator explicitly
# so older pip resolvers don't pick a conflicting version.

fastapi==0.110.1
uvicorn[standard]==0.25.0

# MongoDB (motor pulls pymongo as a peer; pin both for reproducibility)
motor==3.3.1
pymongo==4.5.0
dnspython==2.8.0          # required for mongodb+srv:// (Atlas) URIs

# Validation
pydantic[email]==2.12.5
email-validator==2.3.0

# Auth
PyJWT==2.10.1
bcrypt==4.1.3

# Env loading
python-dotenv==1.2.2

# Push notifications (Expo)
exponent_server_sdk==2.2.0
requests==2.33.1          # used by exponent_server_sdk

# PDF / Excel / QR / images
reportlab==4.4.10
openpyxl==3.1.5
qrcode==7.4.2
Pillow==12.2.0
